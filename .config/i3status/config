general {
    markup = "pango"
}

order += "cpu_usage"
order += "volume master"
order += "battery 0"
order += "tztime local"

cpu_usage {
    format = "CPU %usage"
}

volume master {
    format = "VOL %volume"
    format_muted = "MUT %volume"
}

tztime local {
    format = "%Y-%-m-%d %I:%M:%S"
}

